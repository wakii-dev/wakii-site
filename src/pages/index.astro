---
import Base from '../layouts/Base.astro';
import Landing from '../components/Landing.astro';
import { en } from '../i18n/landing';
import { SITE_TAGLINE } from '../config';
---

<Base
  title={`wakii — ${SITE_TAGLINE}`}
  description="Wakii is an agentic IDE with a built-in superpowers team — plugin bundled, kit auto-installed, on by default."
>
  <Landing strings={en} />
</Base>
