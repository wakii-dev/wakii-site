# Improvements Log — superpowers workflow (in-flight flags + end-of-task learnings)

## 2026-09-04 — SF-2 Landing (FI-291)

- **`orca linear comment add` trả `ok:false` thật cho body dài (~2.3KB qua heredoc stdin)** — không phải false-negative như `save-issue` (comment KHÔNG apply). Workaround: viết body ra file rồi `--body-file <path>` → ok:true. Ghi chú: probe bằng marker comment để rà trạng thái gây ra 1 comment rác "**test marker probe**" trên FI-291 (CLI không có comment delete — chấp nhận). Suggested change: orca-bridge/skill nên ghi rõ stdin-heredoc không đáng tin cho body dài; và cần `comment delete` cho cleanup.
- **`orca linear issue <id> --comments` latency:** comment mới post cần vài giây để visible qua API — `story-verify` B3 chạy ngay sau khi post sẽ đọc nhầm OUTBOX. Suggested change: story-verify có thể retry 1 lần sau 3s trước khi kết luận OUTBOX.
- **CSS grid min-content trap:** bento grid 1fr track bị kéo to theo content cố định (bracket canvas 680px) dù có auto-scale script (scale hình nhưng không co layout). Fix pattern: `min-width: 0` trên grid items. Nên ghi vào frontend-design/design skill notes cho mockup-kit layouts.

## 2026-09-04 — SF-3 (FI-297)
- **Stale browser cache sau rebuild (astro preview)**: Orca embedded browser phục vụ HTML cũ sau khi `astro build` ghi lại dist — computed-style measurement (h2 21.75px) mâu thuẫn với screenshot (đã styled) và khiến code-reviewer round 1 báo P1 mechanism-sai. Fix quy trình: cache-bust (`?v=N`) hoặc reload trước khi đo; khi browser measurement mâu thuẫn dist artifacts → tin dist (deterministic) + re-measure cache-busted. (Liên quan: reviewer có thể "live-verify" trên stale state — brief reviewer về cache gotcha.)
- **Astro `inlineStylesheets: auto`**: CSS bundle nhỏ được inline vào HTML head — grep dist/_astro/*.css KHÔNG đủ để kết luận "thiếu styles"; phải grep cả inline `<style>` trong dist/**/*.html.
- **Anchor click + `scroll-behavior: smooth`** trong embedded browser: hash đổi nhưng không scroll (browser thật OK). Đã flag epic cho SF-5; nếu cần verify anchor trong embed → set `scrollBehavior='auto'` tạm rồi click.
- **em-dash split off-by-one**: `indexOf(' — ')` + `slice(i+1)` giữ lại em-dash → dùng `split(' — ')`. Bug thật ("— —" trên landing note), bắt được nhờ Rule 0 flow walkthrough.

## 2026-09-04 — SF-5 Convergence (FI-298)
- **Reduced-motion mid-session P2**: nếu user bật `prefers-reduced-motion` SAU khi trang đã load với motion ON, `.anim` đã add → `.anim [data-reveal] {opacity:0}` giữ elements ẩn (initMotion đã chạy, observer vẫn reveal khi scroll nên thực tế self-heal trên scroll; chỉ ảnh hưởng elements ngoài viewport đầu chưa intersect). Suggested hardening: trong global.css RM media query thêm `.anim .reveal, .anim [data-reveal] { opacity: 1 !important; }`.
- **Canonical swap per locale (verdict b)**: Base.astro canonical cũ build từ `canonicalPath` prop → dễ lệch thật-URL. Đổi sang self-referencing `new URL(pathname || '/', Astro.site)` — mỗi locale tự canonical (Google guidance cho hreflang cluster). Verdict pinned trong plan Task 4; verify script phải so trailing-slash (không `/index.html`).
- **LangSwitcher hash preserve**: anchor hash EN (`#philosophy`) KHÔNG map slug VI nhưng giữ vẫn better-than-drop (browser đứng top thay vì chết link). Verdict pinned trong plan Task 5; e2e xác nhận hash sống qua switch.
- **`npm install` tạo `package-lock.json` untracked** trong repo dùng `pnpm-lock.yaml` — KHÔNG commit (sẽ phá lockfile contract). Nếu cần build trong worktree, xóa lockfile sau hoặc dùng `npm ci --no-save`.

## 2026-09-04 — Story 3 SF-1 (FI-301)
- **story-verify chọn nhầm bracket sau khi app rename Orca→Wakii**: script hardcode `/opt/homebrew/bin/orca` + probe `ORCA_USER_DATA_PATH` (orca-dev/orca) — trên máy hiện tại CLI thật là shim `/usr/local/bin/orca` (Wakii.app), override data path khiến `ok:false` → `WT_META` rỗng → rơi về bracket glob alphabet-first → B3/B4/B5 đọc nhầm story FI-289 (review:FI-290, dest:fi289) dù orca metadata đúng (linkedLinearIssue FI-301). Fix đã patch vào `~/.claude/bin/story-verify`: ưu tiên shim, gọi TRỰC không override data path. Suggested: các script story-* khác (watchdog/resume) nếu có cùng pattern ORCA_BIN/data-path thì patch tương tự.
- **`orca worktree set --linear-issue` nên chạy NGAY khi vào SF** (trước cả code) — metadata là nguồn bracket-resolution của story-verify; set muộn = gate đọc nhầm story (gặp ở run này).
- **eval qua `orca exec` (agent-browser): 3 bẫy spacing** (mở rộng gotcha SF-2 cũ): ngoài tách theo space + strip double-quotes, arrow **block body** `()=>{...; x}` trả undefined (phải expression body hoặc statement cuối là call), và `let x`/`return x` đều chứa space → dùng expression thuần: `eval Array.from(...).flatMap(...).map(...).join(...)`.

## 2026-09-05 — Story 3 SF-3 (FI-303)
- **iframe probe @width: tab `about:blank`/`data:` URL = opaque origin** → iframe trỏ localhost là CROSS-ORIGIN, `contentDocument` ném SecurityError → `orca exec eval` trả output RỖNG (không phải JSON error). Fix: host trang probe cùng-origin trong `dist/__probe.html` (preview server serve tĩnh) rồi mở tab đó — eval trên tab probe đo `window.f.contentDocument` bình thường. Bổ sung cho gotcha iframe-probe FI-298.
- **Headless shell screenshot full-page dọc**: `--window-size=1280,6800` render cả trang nhưng `.reveal` ngoài viewport đầu giữ opacity 0 (IntersectionObserver không fire khi không scroll) — không phải bug layout, là reveal contract; visual tầng chỉ đáng tin trên browser thật có scroll (Orca tab) hoặc sau khi reveal đã chạy.
- **Screenshot flag-state ảo sau rebuild**: tab Orca trỏ URL cũ (?v cũ) vẫn hiện state trước rebuild nếu không cache-bust — mọi lần rebuild xong mở tab `?v=<số mới>` (đã có trong gotcha FI-297, tái xác nhận lại ở SF này khi mock-flip 2 lượt).

## 2026-09-05 — Story 3 SF-4 Convergence QA (FI-304)
- **P1 reveal-cleanup re-hide (Rule 0 bắt được, prod đang dính)**: motion.ts cleanup `animationend` remove class `reveal`/`reveal-in` nhưng KHÔNG remove attribute `data-reveal` → selector `.anim [data-reveal] {opacity: 0}` re-apply sau entrance → mọi element attribute-based (6 cells /download, toàn bộ catalog /skills EN+VI, workflow gates, faq details) TẮT NGÔM ~0.6s sau khi reveal, chỉ khi tab focused đủ lâu để animation thật sự chạy xong. Element class-based (`.reveal` trên landing) không dính vì remove class = hết match selector ẩn. Fix 1 dòng: `el.removeAttribute('data-reveal')` trong cleanup. Bài học: (a) shared util thêm selector variant (`[data-reveal]`) thì cleanup phải đối xứng; (b) bug chỉ visible khi browser focused + chờ > animation duration — screenshot nhanh hoặc tab unfocused (animation timeline đóng băng) đều GIẤU bug; Rule 0 phải đo lifecycle đầy đủ (reveal → animationend → sau-cleanup), không chỉ snapshot 1 thời điểm.
- **Webview không focus = animation timeline đóng băng nhưng `animation-play-state` vẫn báo `running`**: đo `animationName: bxreveal / playState: running / opacity: 0` sau nhiều giây — dễ kết luận sai "animation không apply". Thật ra clock bị throttle vì window nền. Fix quy trình: `osascript -e 'tell application "Wakii" to activate'` trước khi đo animation; hoặc kết luận trên getComputedStyle SAU khi forced class-toggle (simulation) thay vì chờ event.
- **Audit script regex phải khoan dung `data-astro-cid-*`**: Astro render chèn attribute giữa class và `>` (`<div class="kicker" data-astro-cid-xxx>`) — assertion chuỗi cứng `<div class="kicker">text</div>` báo FAIL giả. Dùng `<div class="kicker"[^>]*>text</div>`. Tương tự với Soon-cell: `'soon-cell' in html` match cả CSS rule inline → phải match `class="bx sp4 soon-cell"` usage cụ thể.

## 2026-09-04 — SF-4 / FI-293
- SF-1 task "og-base" chưa từng landing (không có OG meta ở bất kỳ page nào) — phát hiện ở convergence. Bài học: task tick trong SF trước phải verify bằng grep dist, không tin commits.
- story-verify B3 chỉ nhận literal "VERDICT ... APPROVED" trong Linear comment — code-reviewer agent thường chỉ nói "APPROVED"; reviewer prompt nên yêu cầu post đúng format hoặc coordinator bổ sung.
- Headless Chrome --window-size clamp min-width → báo overflow ảo ở mobile; iframe probe (same-origin, w=390, đo scrollWidth) là reliable.
- Bash tool strip secret literal khỏi command line → pass secret qua file + $(cat).
- Vercel preview mặc định bật SSO protection → preview link không xem public được nếu không tắt qua API (PATCH /v9/projects ssoProtection:null).

## 2026-09-08 — SF-2 / FI-361 (ritual, đã xử lý — resolved)
- **`orca linear comment add` pipe sang jq chết giữa chừng → double-post**: jq không có trên máy → pipe fail SAU khi orca đã post; retry tạo comment trùng (CLI không có delete). Rule: KHÔNG pipe orca output; verify sau bằng `orca linear issue <id> --comments --json` và parse `result.comments` (KHÔNG phải `result.issue.comments` — path khác → "0 comments" ảo), có latency vài giây. [resolved — pattern ghi memory]
- **orca exec eval — zero-quote-glyph rule mở rộng**: source JS phải chứa 0 ký tự quote, KỂ CẢ string format kiểu `join(' | ')`; `#` trong CSS attribute selector (`a[href^=#]`) KHÔNG hợp lệ → selector phải build runtime kèm charcode 34. Node generator sinh JS (S() encoder + Q runtime-quote) hoạt động lần đầu sau fix. [resolved — pattern ghi memory]
- **Generator eval flow chuẩn cho blog walkthrough**: `goto` trên page cũ thay vì tạo tab mới (tab create không auto-target); clickexact `a[href="<url>"]` cho nav link; TOC jump proof = click → decodeURIComponent(hash) → scrollIntoView instant → so scrollY==offsetTop. [resolved — dùng lại SF-3/4/5]

## 2026-09-08 — SF-4 / FI-363 (ritual, đã xử lý — resolved)
- **Evidence-pack sai 2 chỗ bị reviewer bắt ở SF-4 (P0)**: (1) D3 ghi spec fi338 "đã có trên main" — thật ra 404 (chỉ local mirror; `gh api` = 32 specs, 0 dispatch-queue); (2) D8 skills "21/14" là grep thô đếm nhầm dòng interface `id: string;` + comment header — số thật **20/13** (skills.astro comment xác nhận 13). Bài #16 tự vệ bằng honesty-note (không link, không quote); #19 ghi 20/13 + "tại thời điểm viết" theo rule D8. Rule cho snapshot tương lai: **snapshot = RAW OUTPUT lệnh gốc, không paraphrase qua lớp nào**. [resolved — đã comment Linear FI-363 cho SF-5 sửa pack; bài đã trung thực]
- **Anachronism ngày-sự-kiện trong bài backdate**: bài pubDate 09-03 kể merge xảy ra 09-07 → không gắn mốc ngày sự kiện trong prose (chỉ hash + số PR, ngữ cảnh dùng dạng tương đối). plan-critic bắt trước Phase 4. [resolved — patch plan T1]
- **Eval-JS generator — escape `\"` gãy regex encode**: template dùng single-quote ngoài / double-quote trong (không escape); kết hợp rule SF-2 (0 quote glyph, `return(...)`, IIFE-param thay `var`, trim template). Flow recipe thêm: ports 4321/4322 hay bị session khác chiếm → curl route trên port thực tế trước khi verify; tab background screenshot TIMEOUT kể cả sau activate → pixel qua headless Chrome, nhưng fragment-URL headless = blank-paint quirk → dùng --dump-dom cho anchor proof. [resolved — pattern ghi memory]

## 2026-09-08 — SF-3 / FI-362 (ritual, đã xử lý — resolved)
- **Fingerprint dist trước khi verify trên localhost**: ports 4321/4322/4323 ĐỀU bị session song song chiếm (mỗi cái serve dist KHÁC — 200 + title đúng site nhưng là dist của SF-4); bind fail âm thầm khi background subshell. Rule mở rộng từ SF-4 ("curl route trước"): sau khi bind server verify, curl page + đếm marker ĐỘC QUYỀN của dist mình (vd 7 slug mới) + grep contamination (slug series khác) — 200 không phải là của bạn. [resolved — pattern ghi memory]
- **Iframe probe flow-evidence (same-origin) — load-phase state machine**: headless fragment-URL = blank (SF-4 đã ghi); thay bằng probe page serve TỪ cùng server: iframe load bài với #fragment, state machine theo load N (jump→click cross-link→landed→click back→listing), chờ smooth-scroll bằng setTimeout-settle (rAF CHẾT dưới --virtual-time-budget — 2 lần probe fail vì rAF); kết quả paint vào DOM probe → 1 screenshot = bằng chứng anchor jump + click chain + listing. Screenshot số liệu cần window đủ cao. [resolved — recipe ghi memory]
- **Merge-ngược race thật: dest nhích GIỮA merge-into và update-ref**: dest 4a2fc98→9681a37 giữa build và commit-tree → guard CAS chặn (đúng thiết kế); loop: `git merge dest` lại ("Already up to date" = tree đã hút dest mới), re-read CAS theo SHA mới, commit-tree + 2 ancestor guards. Không bao giờ update-ref khi CAS fail. [resolved — guard đã hoạt động như thiết kế]
- **Astro glob-loader WARN "Duplicate id" sau merge nhiều SF**: no-op thực tế (file overwrite chính nó) — verify output trực tiếp (dist N slug duy nhất, listing card unique, 1 h1/post); flag epic cho SF-5, không sửa loader. [resolved — flag Linear FI-362 Phase 5 comment]

## 2026-09-08 — SF-5 / FI-364 (ritual, đã xử lý — resolved trừ 1 open)
- **Crash-path stderr bypass home-scrub (P1 security — pattern lặp lần 2)**: audit/report script có output dán vào Linear: `scrub()` chỉ bọc happy-path `out()` là KHÔNG ĐỦ — uncaught exception in raw stack kèm `/Users/hoivu` (throw site + stack, 2 chỗ). Fix pattern: `process.on('uncaughtException'+'unhandledRejection')` route qua scrubber + guard shape response API ngoài (`Array.isArray`) trước `.map`, + allowlist path capture trước khi interpolate vào URL. [resolved — fix `3fa5827`, security-audit bắt, re-run sạch]
- **`orca click --element @ref` không navigate trên card link**: 2 attempt (retry + scrollintoview trước) vẫn ở nguyên listing; `eval document.querySelector('a[href="…"]').click()` điều hướng thật trong tab (mở rộng recipe clickexact SF-2/4). FLOW proof = DOM navigation thật trong tab Orca; pixel vẫn headless Chrome. Thêm evidence: `orca screenshot` TIMEOUT cả khi app frontmost + tab active (SF-4 chỉ ghi tab background) → headless Chrome là pixel path CHÍNH, không chỉ fallback. [resolved — recipe ghi memory story-fi359]
- **Convergence QA phải recompute ground-truth numbers từ tree, không tin số trong pack**: pack check-1 ghi "15 slug × 2 / 30 posts / 60 file" — số học sai (thật: 25 slug × 2 = 50 file; "30 posts" đúng theo quy ước 10 seed posts + 20 bài mới); chạy đúng số pack = false-FAIL dest sạch hoặc executor tự "sửa" tiêu chí (mất integrity audit). plan-critic P0 bắt trước Phase 4. [OPEN — đề xuất patch story-workflow SKILL phần convergence SF: "pinned numbers trong pack phải derive lại từ parity gate/lint scope thật; lệch = spec-NOTE lên epic, không phải content-fail" — chờ user duyệt, không self-edit skill lớn]
- **grep -c trên file 1-dòng (rss.xml build) luôn = 1** bất kể số occurrence → cross-check đếm phải `grep -o pattern | wc -l`. [resolved — memory story-fi359]

## 2026-09-07 — Story 5 SF-1 Content infra (FI-355)
- **Node TS-stripping floor 22.18 vs CI pin**: build-time check script import thẳng `.ts` (node strip types native — tiện, khỏi thêm dep), local node 24 nhìn xanh nhưng CI/deploy pin `node-version: 20` → `ERR_UNKNOWN_FILE_EXTENSION` exit 1 (reviewer reproduce). Floor thật: 22.18 (backport) / 23.6 (unflagged). Pattern: thêm node-script-import-.ts ⇒ sweep TẤT CẢ node pins trong .github/workflows + set `engines.node` machine-readable.
- **Test-harness shape mismatch**: assert script build entry FLAT (`{category, draft, ...}`) trong khi util expect entry-shape (`{id, data:{...}}`) — TypeError ngay lần chạy đầu (verify trước commit bắt được, chưa kịp commit). Pattern: parse helper phải trả đúng shape consumer expect, không phải shape tiện cho assert.
- **Multi-session port đụng**: story song song (FI-349 SF-1 + FI-359) cùng máy — ports 4321/4322 bị chiếm, `astro preview` tự nhảy 4323 → KHÔNG hardcode port, đọc log server; `pkill -f "astro preview"` nguy hiểm trúng process session khác (lần này không trúng vì chúng chạy `astro dev`) — kill bằng port/PID riêng.
- **story-verify B1 `test|spec.` regex** không match `check-blog-utils.mjs` → B1:WARN dù build đã có 2 assertion gates — WARN là precedent bình thường với static site (asserts thay thế), đừng thêm file rỗng chỉ để tắt WARN.
- **Hero pipeline proven (deterministic)**: SVG template 1200×630 → `chrome --headless=new --screenshot --window-size=1200,630` render ĐÚNG 1200×630 (không dính clamp — clamp chỉ ở window-size nhỏ 390) + IHDR check trong script fail-loud; CVDisplayLink ERROR trên macOS là noise, PNG vẫn written; SVG source commit cạnh PNG cho review + tái tạo (`node scripts/render-blog-heroes.mjs`).
- **update-ref old-value pin**: `git update-ref refs/heads/<dest> <new> <expected-old>` là guard miễn phí chống race khi story khác cùng đụng repo (dùng kèm 2 guards ancestor + tree sạch theo merge-playbook).

## SF-3 (FI-357) learnings — 2026-09-07
- **Rare-branch probe pattern (mutate→shoot→revert)**: verify nhánh hiếm không commit được dữ liệu (featured hero-img khi post thật không có hero; empty-state khi không category rỗng) — sed tạm frontmatter → `pnpm build` → orca eval+screenshot → `git checkout -- src/content/blog/` → rebuild sạch. Bằng chứng visual thật, working tree không bẩn, parity gate vẫn serve (mutate cả EN+VI khi field có schema-parity). Sẽ tái dùng ở SF-4 QA.
- **Orca browser CLI flags (refine)**: `orca eval` dùng `--expression` (KHÔNG positional/`--expr`); `orca screenshot` KHÔNG có `--path` — chỉ `--json` → decode `result.data` base64 (pattern đã ghi 2026-09-07 — flag name là phần mới). `orca linear status set` vẫn `--id X --to Y`.
- **i18n count strings**: hàm đếm trong strings map (`${n} posts`) cần singular guard ngay từ đầu — reviewer bắt "1 entries" render thật trên category 1-post; EN phải tested N=1, VI bất biến.

## SF-2 (FI-356) learnings — 2026-09-07
- **Verification-script edits cần review kỹ như code**: checker "FAIL" trên page ĐÚNG 3 lần liên tiếp — (1) regex không khoan dung `data-astro-cid-*` (gotcha đã có), (2) biến group aria-label thành non-capturing làm content trượt từ [2]→[1], (3) check tự mâu thuẫn (flag chính link /vi/blog/ hợp lệ). Bài học: dist HTML đúng → dump + đọc markup THẬT trước khi sửa code; sửa checker xong re-run toàn bộ, đừng tin diff "nhỏ nên chắc chắn đúng".
- **Layout-owns-shell pattern cho trang song ngữ**: chuyển toàn bộ article surface (hero/meta/prose/related/CTA/pager/JSON-LD) + UI strings record (en/vi) vào 1 layout → route files còn ~24 dòng (getStaticPaths + render + slot); og contract comment đi theo wiring. Không còn mirror EN/VI ~95% như kiểu DocsLayout (docs pages tự render h1/crumb — pattern cũ, đừng copy cho blog).
- **JSON-LD trong body**: `is:inline` + `set:html` (Astro không bundle là inline); escape `<` → `u003c` chống `</script>` break-out từ content tương lai (review P2 — JSON.stringify KHÔNG escape `<`). schema.org chấp nhận JSON-LD anywhere trong body — khỏi đụng Base head.
- **Dest moved mid-flight (SF-3 merge trước)**: merge-ngược dest mới vào sf-branch TRƯỚC update-ref, rồi re-run build + DOM asserts trên cây TÍCH HỢP (37 pages) — hai SF sạch vì touch map rời nhau (SF-3 listing/category, SF-2 detail). update-ref dùng old-value pin (`git update-ref refs/heads/<dest> <new> <expected-old>`).
- **Reveal lifecycle qua headless**: `--virtual-time-budget=10000 --dump-dom` cho thấy trạng thái SAU animationend — element in-view bị cleanup sạch class (đúng contract), element dưới fold giữ `.reveal` chờ IO (không phải bug) — phân biệt được "cleanup hỏng" vs "chưa scroll"; `.anim` trên `<html>` chứng minh initMotion đã chạy qua guard reduced-motion/coarse-pointer.

## SF-4 (FI-358) learnings — 2026-09-07
- **Aggregator .ts import không dùng được cho bare node**: `src/components/blog/categories.ts` import extensionless `from '../../i18n/categories'` (Astro/Vite resolve OK, node ESM ERR_MODULE_NOT_FOUND — gặp live). Pattern cho node QA scripts: import trực tiếp module LÁ import-free (`src/i18n/categories.ts`) rồi derive lại (`Object.keys(CATEGORY_LABELS.en)` = đúng định nghĩa BLOG_CATEGORIES) — giữ derive-from-enum contract không phải sửa file SF-3-owned.
- **Fresh worktree + `vercel link` side-effect**: link tạo `.env.local` + APPEND `.env.local` vào `.gitignore` của repo → `git checkout -- .gitignore` ngay (giữ branch sạch); `.env.local` untracked để yên (auth CLI, không commit).
- **404 page = exemption đặc biệt cho SEO/crawl audit**: Base render canonical `/404/` + hreflang `/404/`,`/vi/404/` trên 404.html — non-routes, nhưng page noindex (không bao giờ index) → canonical/hreflang moot; lang-switcher trên 404 trỏ non-route là pre-existing cosmetic (không có "đúng" cho switch ngôn ngữ của trang không tồn tại). Audit scripts phải exempt 404 (cluster + canonical self + crawl) + ghi epic note, đừng fix code (boundary QA).
- **Crawl scope = `<a href>` thôi**: lấy MỌI `href="` dính cả `<link rel=alternate>` (hreflang/RSS metadata) → false positive; navigation thật mới là scope "link không 404". Metadata links thuộc audit hreflang riêng.
- **Reduced-motion audit 2 chiều**: RM-only dump chưa đủ chứng minh gate hoạt động (class vắng có thể do JS chết) — phải đo CẶP `--force-prefers-reduced-motion` vs KHÔNG flag trên cùng page: NRM có `.anim` trên `<html>` + RM không = gate thật; kèm screenshot RM thấy content visible. 5/5 pages pass.
- **Lighthouse baseline-vs-post trên deployed preview**: cùng page set, 2 deploy preview URL khác nhau — baseline 88/86/98 vs post 96/96/98 (noise cold-cache ở baseline) — ±17 criterion dư sức; đo ngay đầu SF trên base commit là đúng quy trình, đừng đợi xong code mới đo (không có "trước" để so).

## SF-1 (FI-374) learnings — 2026-09-08
- **Merge qua temp worktree: ATTACH branch, đừng `--detach`**: `git worktree add --detach <tmp> <dest>` + merge = merge commit rơi trên detached HEAD, branch ref KHÔNG nhích (phải `git branch -f` sau, phát hiện bằng `git rev-parse <dest>` ngay sau merge). Đúng: `git worktree add <tmp> <dest>` (attached) → merge cập nhật ref trực tiếp. Sẽ lặp ở mọi SF merge (SF-2..6).
- **Fixture-prove fix review TRƯỚC khi commit**: fix P1 verify-shipped lần 1 (same-line nearest-label) vẫn còn bug layer 2 — SOFT `/i` match chính canonical uppercase ("ROADMAP") trong window ±120 và markdown prose = 1 paragraph 1 dòng nên 2 câu cùng dòng mượn nhãn nhau. Chạy fixture 4-case (compliant/mismatch/soft/bare, chủ tình gộp 1 dòng) trước commit đã bắt hết → matcher claim-scoped phải **sentence-scoped** (`[.!?;]+(?=\s|$)`) + soft phải skip exact-uppercase-canonical. Fixture gài multi-mention-one-line là case bắt bug, không phải noise.
- **Chạy old-script version để test phải trong repo tree**: script derive `root` từ `import.meta.url` → chạy từ /tmp cho `0 enforced files` + ENOENT giả. Copy vào `scripts/` tạm, chạy, xoá.

## SF-4 (FI-377) learnings — 2026-09-08
- **Runbook "VI KHÔNG khai heroImage" MÂU THUẪN parity gate (OPEN — cần coordinator)**: `scripts/check-blog-slug-parity.mjs` (LOCKED FI-349) yêu cầu heroImage presence AND value sync CẢ HAI locale; 4 seed precedent đều có VI heroImage. Lần build đầu theo runbook = FAIL "heroImage declared in en only". Fix đúng = thêm dòng heroImage giống hệt vào VI (render script chỉ quét EN — no side-effect). Đề xuất: sửa runbook §Hero wiring + plan template wording ở đợt SF sau, KHÔNG self-edit (editorial docs FROZEN). [OPEN]
- **Product docs công khai (wakii-dev/wakii) mang branding upstream "Orca" + link stablyai/App Store**: nguồn cross-check tuyệt vời cho cơ chế (install/ssh/mobile/linear/skills/troubleshooting/ways-to-run/recipes), nhưng guide Wakii chỉ được TRÍCH cơ chế + ghi nguồn+ngày, KHÔNG copy URL upstream vào bài. Pattern: tải docs vào /tmp qua `gh api contents`, cho executor đọc từ đó.
- **Transcripts trong plan stale theo thời gian**: §A.3 (kit story-* CLIs) stale sau khi kit re-sync giữa story — bài phải embed live capture MỚI kèm ngày (executor re-run lệnh read-only trước khi nhúng), đừng embed từ plan cũ. Plan transcripts = nguồn khởi điểm, không phải truth vĩnh viễn.
- **Same-origin iframe probe trên dist/ = FLOW check không cần orca browser**: orca browser tab load localhost bị `chrome-error://chromewebdata` (block) — fallback đã verify: đặt probe wrapper HTML vào dist/ (gitignored, same-origin với site) → headless Chrome --dump-dom đọc kết quả; click-chain thật (lang-switch, related) qua `dispatchEvent(click)` trong iframe; mobile 390 = iframe 390px + đo `docScrollWidth` + `overflowTags` (code block scroll nội bộ = thiết kế, không phải overflow trang). Mở rộng pattern FI-359 SF-3.
- **Claims review-only lớp phải quét PARAPHRASE, không chỉ literal**: lint chỉ grep literal — security-audit bắt "Pairing is one-time / the desktop is always the source of truth / reconnects automatically" (quote NGUYÊN VĂN từ product docs) = claim persistence bị cấm theo FI-341 (Wakii ≠ upstream verify), "kể cả paraphrase" trong registry đúng nghĩa. Fix = trim về 5 nhãn ALLOWED (QR-gesture + 4 năng lực); không bao giờ promote claim từ docs upstream mà không verify trên app Wakii.
- **Preview port đụng giữa SF song song**: 4321 bị SF khác chiếm, `astro preview` tự nhảy 4323 — đọc log server lấy port thật; probe files bỏ dist/ (gitignored) giữ tree sạch.
## SF-3 (FI-376) learnings — 2026-09-08
- **Fan-out 8 task-executor qua Agent tool vs rate limit**: 429 là chuẩn khi 8 song song (5/8 fail, sonnet lẫn opus) — pattern: resume theo SLOT (khi có completion thật, không resume ngay sau fail), giữ ≤6 concurrent; agent fail 2 lần cùng task → coordinator viết inline thay vì resume tiếp. Brief agent phải kèm "tự đếm band bằng ĐÚNG method lint (prose bỏ fenced, split whitespace) trước khi báo xong" — T2 nộp 1728 từ vì đếm sai phương pháp; nhắc lại lần 2 thì hết.
- **Commit-message number honesty**: 2 lần trong 1 SF ghi số band CHƯA verify vào commit message ("1397"/"1399" khi thật 1419/1400) — số trong message phải extract bằng lệnh + in ra trước rồi mới ghi; phát hiện sai → amend ngay (message sai số = báo cáo không trung thực, kể cả nhỏ).
- **Runbook §Hero wiring STALE so với parity gate**: runbook ghi "VI KHÔNG khai heroImage (share hero EN)" nhưng `check-blog-slug-parity.mjs` đòi heroImage presence+value ĐỒNG BỘ cả 2 locale (batch-1 heroes đều khai 2 locale) → build đỏ giữa SF, fix = wire VI mirror (`1816541`). Editorial docs frozen → flag coordinator sửa runbook (đã ghi trong audit comment FI-376).
- **render-blog-heroes.mjs không có --help**: flag duy nhất là `--check`; chạy script với arg lạ (--help) = RENDER THẬT (side effect; may là output deterministic byte-identical nên tree sạch). Đọc script trước khi chạy tool có side effect.
- **Orca webview synthetic click**: a11y click (--element ref) navigate được CHỈ khi element trong viewport — card below-fold miss im lặng, post-page header link cũng miss (listing header thì được). Pattern Rule 0 cho site tĩnh: click-chain thật cho phần in-viewport (listing→card→post, lang-switch listing) + DOM href verification deterministic cho phần còn lại (8/8 × 2 chiều lang-switch, 0 broken links) — nói thật ranh giới click-vs-DOM trong báo cáo.
- **story-verify B2 plan-name collision**: tên plan chứa "sf3" khớp cả plan cũ story khác (`2026-09-07-sf3-series-b...`) — gate tiebreak newest-mtime hoạt động đúng nhưng giòn; đặt tên plan luôn tiền tố date + issue (`2026-09-08-fi373-sf3-...` đã đúng).
