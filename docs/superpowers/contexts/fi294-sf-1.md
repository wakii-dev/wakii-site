# SF-1 Context Pack — Content foundation + shared motion (FI-294)
> Đọc file này THAY VÌ tự tổng hợp từ bracket + epic + comments.
> Epic: FI-294. Epic spec: docs/superpowers/specs/2026-09-04-site-content-depth.md.
> Bracket: docs/superpowers/brackets/fi294-site-content-depth.md. Tier 0 — KHÔNG có deps.

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)
1. **Skills data module**: tạo typed data module (pattern theo `src/i18n/landing.ts`:
   interface + 2 objects EN/VI trong 1 file, compiler ép đủ 2 locales). Nguồn:
   frontmatter thật của 20 skills trong `~/.claude/skills/*/SKILL.md` (name +
   description). Mỗi skill: {id, name, command (slash-command), category,
   desc_en, desc_vi, how_en, how_vi} — desc = mô tả 1 câu, how = cách làm việc
   2-3 câu. Tên skill + command giữ tiếng Anh, KHÔNG dịch.
2. **Skills triage gate**: duyệt 20 skills — loại platform-specific/internal không
   public được khỏi catalog (hint: `computer-use`, `orchestration`, `orca-cli`,
   `orca-bridge` là Orca-app-specific — cân nhắc; quyết định cuối là list trong
   notes). Ghi list + taxonomy category vào notes SF-1. **Sequencing pin: data
   modules build ĐỦ 20 skills (SF-1 exit KHÔNG phụ thuộc confirm); SF-2 render
   theo triaged subset; confirm PM là gate cho SF-2 START** (coordinator
   escalate sau khi SF-1 done).
3. **Roadmap data module**: Now/Next/Later đúng nội dung §4 spec (đã duyệt lúc
   APPROVE — đọc spec §4 "Roadmap content"); milestones theo khung thời gian mơ
   hồ (Q4 2026, 2027), không ngày cụ thể xa.
4. **Philosophy copy (landing strings)**: copy rút gọn 8 trụ cột cho landing
   section (mỗi pillar: tên + 1 câu), thêm vào `LandingStrings` interface + EN +
   VI. Cộng copy deepen workflow (SF-4 wiring, nhưng KEYS + copy định nghĩa ở
   đây: cách 9 agents + gates B0-B5 làm việc). Copy đầy đủ vẫn chỉ ở
   `story-workflow.md` — landing chỉ rút gọn (DRY).
5. **Motion util extract**: tách reveal-on-scroll/stagger logic từ
   `src/components/Landing.astro` `<script>` (dòng ~860-1002: IntersectionObserver
   + stagger 60ms + `.reveal`/`.reveal-in` + gate `.anim` khi motion OK) thành
   util dùng chung (script module hoặc init function nhận selector/data-attrs).
   Contract: data-attribute driven (`data-reveal`...), tự respect
   `prefers-reduced-motion` + coarse pointer NHƯ hiện tại. Landing refactor xong
   phải render + animate GIỐNG HỆT trước (regression check bằng mắt).
6. **Nav/Footer links**: thêm Skills + Roadmap vào `src/components/Nav.astro`
   (desktop links, locale-prefixed) + `src/components/Footer.astro`. ≤900px theo
   collapse hiện có (không làm mobile menu — ngoài scope).
7. **Kit counts sync**: sửa "17 skills / 23 story-* CLIs" → "20 skills / 24
   story-* CLIs" trong `agents-and-kit.md` CẢ EN lẫn VI (description frontmatter
   + body) + grep mọi chỗ khác nhắc 17/23.
8. **i18n keys pre-add ĐẦY ĐỦ**: thêm TOÀN BỘ keys mới vào `LandingStrings`
   interface + `en` + `vi` — gồm philosophy (8 pillar) + workflow-deepen (9
   agents + gates B0-B5) + get-wakii (download section). SF-3/SF-4 KHÔNG sửa
   landing.ts (chỉ wiring markup trong component riêng) → hết conflict song song.
9. **Landing componentize**: tách sections của `Landing.astro` thành
   `src/components/landing/*.astro` (hero, bento, zero-setup, workflow,
   get-wakii từ quickstart, faq) — Landing.astro thành shell render thuần.
   Ownership sau tách: SF-3 sở hữu get-wakii component; SF-4 sở hữu philosophy
   (mới) + workflow; còn lại read-only. Regression: landing render + animate
   y hệt trước tách.
10. **Lighthouse baseline**: đo perf `/` và `/vi/` TRƯỚC mọi trang/section mới
    (baseline của acceptance #5 epic — ghi số vào notes SF-1).

## Touch map (SF-1 sở hữu)
- `src/data/skills.ts` (MỚI) — hoặc `src/i18n/skills.ts` theo pattern landing
- `src/data/roadmap.ts` (MỚI)
- `src/i18n/landing.ts` — thêm interface fields + en + vi cho TOÀN BỘ keys mới
- `src/components/motion.ts` (MỚI — extract từ Landing.astro)
- `src/components/landing/*.astro` (MỚI — section components tách từ Landing)
- `src/components/Landing.astro` — refactor thành shell render các components
- `src/components/Nav.astro`, `src/components/Footer.astro` — thêm links
- `src/content/docs/en/agents-and-kit.md`, `src/content/docs/vi/agents-and-kit.md`
  — counts fix
- READ-ONLY: `src/components/mockups/*` (SF-1 story-1 sở hữu, không rewrite),
  `src/config.ts` DOC_SLUGS (LOCKED), `src/styles/global.css` kill-switch (không
  đụng, phải respect), `src/content/docs/*/story-workflow.md` (chỉ đọc)

## ACCEPTANCE (user-visible)
1. Nav desktop (EN + VI) thấy link Skills + Roadmap; footer cũng có; click dẫn
   đúng route locale (routes 404 lúc này là BÌNH THƯỜNG — trang do SF-2/3 build).
2. Landing render + animate y hệt trước refactor (tilt, parallax, reveal không
   mất); reduced-motion vẫn tắt sạch.
3. Data modules tồn tại với đủ skills (đã triage) + roadmap Now/Next/Later; VI
   song song EN; build xanh.
4. Docs agents-and-kit (EN+VI) hiển thị 20 skills / 24 CLIs.
5. Notes SF-1 (Linear comment hoặc file) ghi taxonomy category + triaged list.

## Boundary (KHÔNG làm)
- KHÔNG build trang /skills, /roadmap (SF-2/SF-3) — kể cả skeleton route.
- KHÔNG đụng section markup/copy hiện có của landing ngoài wiring keys mới.
- KHÔNG thêm animation primitive mới (chỉ extract; mở rộng là quyền SF-5).
- KHÔNG sửa getting-started/quickstart copy (SF-3).
- KHÔNG đụng DOC_SLUGS, REPO_URL, hreflang math (SF-2/3 xử lý cho route mới).
