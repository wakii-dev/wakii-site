# SF-1 Context Pack — Research infra: matrix + claims third-party

> Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch3-repos-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi383-blog-batch3.md`.

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)

1. Matrix batch-3: file mới `docs/superpowers/editorial/2026-blog-longform/topic-matrix-batch3.md` — 50 rows table-format `| N | \`slug\` | cat | pubDate |` (slug pattern `deep-dive-<owner>-<name>`, category `tech` trừ guides→tutorial nếu tách, pubDate 10-01→10-25 2/ngày). 50 repo = landscape 51 − watermarks-remover (danh sách 7 facet trong spec MATRIX section).
2. `scripts/check-blog-content.mjs`: append `topic-matrix-batch3.md` vào `MATRIX_FILES` (dòng ~61).
3. **Lint marker scoped batch-3**: bài thuộc matrix batch-3 THIẾU heading `## Wakii học được gì` (vi/) / `## What Wakii learns` (en/) → lint exit 1; 64 bài batch-1/2 + 5 seed miễn (scope theo matrix origin — `planned.set(..., { file })` đã track). Heading level `##` exact.
4. **Scoped-FORBIDDEN mechanism** (CẢ `check-blog-content.mjs` VÀ `audit-blog-convergence.mjs` T3a): claims-registry hỗ trợ entry scoped per-slug `- "open-source" — scope: <slug-list>` — FAIL chỉ trên slug trong scope; parser hiện dừng ở `## ` đầu tiên → phải mở rộng. Demo 2 chiều: †-repo giả lập FAIL, 8 bài cũ "open source" vẫn xanh.
5. Lint thêm check `fields.pubDate === matrix row pubDate` (bắt sai ngày ở content SF).
6. Audit refresh: T7 want-counts **20/44/50/114** (non-seed per matrix) · T6 SAMPLES theo facet batch-2 · SNAPSHOT skills 20/13 · FAMILIES scope (seed-posts=10/clis=24 giới hạn batch-1/2 HOẶC convention dated — exit = dry-run 1 bài batch-3 chứa "50 bài" không FAIL giả) · `owner()` mapping batch-3.
7. Claims-registry third-party convention: style "theo GitHub API ngày N", license-safe (none/NOASSERTION → "công khai trên GitHub"), FORBIDDEN scoped entries; evidence-pack §Numbers refresh CÙNG commit.
8. `scripts/probe-repos.sh` commit (probe gh api repos: archived/license/stars/pushed — đọc repo list, strip `github.com/` prefix).
9. Style guide: template section "Wakii học được gì" + quy tắc trích dẫn repo (paraphrase license-safe, quote ngắn có attribution).
10. Issue body contract rubric (ADOPT issue: pattern → evidence inline + retrieval date → đề xuất Wakii → upstream links; KHÔNG link path nội bộ site repo).

## Touch map

- W: `scripts/check-blog-content.mjs` · `scripts/audit-blog-convergence.mjs` · `scripts/probe-repos.sh` (mới) · `docs/superpowers/editorial/2026-blog-longform/{claims-registry,evidence-pack,style-guide,runbook}.md` · `topic-matrix-batch3.md` (mới) · `docs/superpowers/editorial/research/digests-batch3/` skeleton (mới)
- READ-ONLY: `src/content.config.ts`, `src/pages/*`, editorial docs sau khi frozen

## ACCEPTANCE (user-visible)

- Bài batch-3 giả lập thiếu marker → lint exit 1 với lỗi chỉ tên marker; bài đủ → pass.
- Bài giả lập claim "open-source" trên repo † → CHẶN ở cả lint lẫn audit (2 scripts cùng kết quả); bài cũ vẫn xanh.
- Matrix parse được: lint biết 50 slug batch-3 + pubDate từng bài; sai pubDate frontmatter bị bắt.
- `scripts/probe-repos.sh` chạy ra bảng 50 repo archived/license/stars/pushed.

## Boundary (KHÔNG làm)

- KHÔNG viết bài (SF-2..5), KHÔNG render hero (content SFs), KHÔNG đổi enum/contracts/RSS, KHÔNG đụng `src/pages/*`.
