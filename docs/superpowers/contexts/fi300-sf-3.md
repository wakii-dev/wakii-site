# Context pack — FI-300 SF-3 Landing Get Wakii upgrade + mobile teaser (Tier 2)

Epic: FI-300 · Spec: docs/superpowers/specs/2026-09-04-site-downloads-mobile.md · Fork từ `story/fi300-downloads-mobile`. Depends: SF-1 + SF-2 (keys + component + hand-off đã merge — KHÔNG thêm key, KHÔNG viết component mới).

## Spec slice
Upgrade section Get Wakii trên landing: download thành primary CTA (flag-aware), build-from-source thành secondary; thêm teaser mobile app (reuse component SF-2); nav-cta repoint theo flag. Design: none — dùng chung hand-off `docs/superpowers/designs/sf-downloads-direction.md` (phần landing/teaser) + design system v2 Bento Premium đang có.

## Touch map
- `src/components/landing/GetWakii.astro` + Landing renderer + copy wiring qua keys SF-1 (i18n) — KHÔNG sửa file keys.
- Nav cta component: theo `DOWNLOADS_LIVE` — true → `/download`, false → giữ `/docs/getting-started/` (G-H).
- Anchor: verify anchor THỰC mà story 2 để lại trong GetWakii section (đọc code trên nhánh fork) TRƯỚC khi ghi deep-link mới — không hardcode `#get-wakii` nếu thực tế khác (G-F). Deep-link nav hiện có phải không gãy.
- Teaser: placement vị trí agent quyết theo hand-off; import `MobileConnect` (hoặc biến thể teaser prop) từ SF-2 — placement-only.
- Entrance: wire qua motion util có sẵn (wire-only).
- Locale-switch pass: / ↔ /vi/ đối chiếu mọi URL mới.

## ACCEPTANCE user-visible (verifier Phase 5 kiểm từng dòng)
1. Landing (EN + VI) Get Wakii: download primary, build-from-source secondary — đúng hierarchy theo hand-off.
2. Flag=false: primary CTA là follow-releases/build path — KHÔNG có nút download chết trên landing.
3. Mock flip `DOWNLOADS_LIVE=true`: primary CTA → `/download` (hoặc trực tiếp asset nếu hand-off quy định) + nav-cta repoint `/download`.
4. Teaser mobile hiện đúng, cùng trạng thái flag với component (false → coming soon, không QR).
5. Deep-link anchor từ nav (cả 2 locale) scroll đúng section — không gãy.
6. Locale switch ở mọi URL mới đổi đúng prefix tương ứng.
7. Reduced-motion + responsive pass trên landing sau thay đổi.
8. Build xanh cả 2 locale.

## Boundary
- KHÔNG sửa /download routes hay component nội dung (SF-2). KHÔNG thêm i18n keys (SF-1 sở hữu — thiếu → FLAG epic). KHÔNG flip flags thật.
- Chi tiết chưa ghi → theo hand-off + tự quyết hợp lý; quyết lớn → FLAG epic FI-300.
