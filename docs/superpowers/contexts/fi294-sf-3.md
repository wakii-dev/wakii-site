# SF-3 Context Pack — Download + Roadmap (FI-294)
> Đọc file này THAY VÌ tự tổng hợp. Epic: FI-294.
> Epic spec: docs/superpowers/specs/2026-09-04-site-content-depth.md.
> Bracket: docs/superpowers/brackets/fi294-site-content-depth.md. Tier 1 — Depends SF-1.
> **Design-first**: bracket ghi `Design: mock-prototype` — designer phase CHẠY
> TRƯỚC dev. Visual authority: sf1-direction.md.

## Spec slice (chỉ phần SF-3 chịu trách nhiệm)
1. **Get Wakii section trên landing**: NÂNG CẤP quickstart section hiện có
   (`#quickstart`, copy ở `landing.ts:244-254`) THÀNH download section —
   build-from-source steps (git clone + make, text-verified khớp
   `getting-started.md`), repo link qua `REPO_URL` constant từ `src/config.ts`
   (PLACEHOLDER-MODE: href == constant là đủ, KHÔNG đòi URL thật), requirements
   (đọc prerequisites từ getting-started). Thay thế, không thêm section trùng.
   Anchor `#get-wakii` (hoặc giữ #quickstart — quyết định khi design, ghi notes).
2. Copy keys get-wakii ĐÃ CÓ SẴN từ SF-1 trong landing.ts (pre-add) — SF-3 chỉ
   wiring markup trong component get-wakii mình sở hữu (không sửa landing.ts).
   Ghi nav-cta decision note lên epic khi xong (giữ "get wakii" →
   /docs/getting-started/ tới khi REPO_URL thật).
3. **Roadmap page**: `/roadmap` (EN) + `/vi/roadmap` (VI) — Now/Next/Later từ
   data module SF-1, đúng NỘI DUNG ĐÃ DUYỆT ở spec §4 (không tự chế item, không
   ngày cụ thể quá xa — milestones Q4 2026 / 2027 style).
4. Entrance animations: TIÊU THỤ motion util SF-1 nguyên trạng.
5. SEO/hreflang cho /roadmap ×2 (same verify như SF-2).
6. Nav-cta decision point: giữ "get wakii" → `/docs/getting-started/` như hiện
   tại (REPO_URL placeholder); GHI note vào epic khi SF-3 done — chuyển REPO_URL
   khi repo public là việc story-1 SF-4 publish checklist.

## Touch map (SF-3 sở hữu)
- `src/pages/roadmap.astro` (MỚI), `src/pages/vi/roadmap.astro` (MỚI)
- `src/components/landing/get-wakii.astro` (tên theo SF-1 componentized — SF-3
  sở hữu, thay quickstart)
- Design hand-off: `docs/superpowers/designs/sf3-download-roadmap-direction.md`
- READ-ONLY: `src/config.ts` (REPO_URL dùng, không sửa), `src/data/roadmap.ts`
  (SF-1), `src/i18n/landing.ts` (keys pre-add, KHÔNG sửa), motion util (tiêu
  thụ), `getting-started.md` (nguồn steps, không sửa), mockups kit, Landing.astro
  shell (SF-1 sở hữu — chỉ swap component tham chiếu nếu cần)

## ACCEPTANCE (user-visible)
1. Landing (EN + VI) có section Get Wakii: thấy steps clone+make, nút/link repo;
   click repo link → href đúng `REPO_URL` constant.
2. Steps trên landing khớp nội dung getting-started (không lệch lệnh).
3. /roadmap + /vi/roadmap từ nav/footer: thấy Now/Next/Later, nội dung khớp
   spec §4, đọc được bằng ngôn ngữ trang.
4. Lang switcher hoạt động 2 chiều cho /roadmap; canonical/hreflang đúng.
5. Section mới reveal mượt qua util; reduced-motion tắt sạch.

## Boundary (KHÔNG làm)
- KHÔNG đụng skills catalog (SF-2), philosophy section (SF-4).
- KHÔNG tạo GitHub releases / binary download (non-goal).
- KHÔNG sửa `REPO_URL` value hay `getting-started.md` content.
- KHÔNG thêm motion primitive mới (SF-5).
- KHÔNG tự chế roadmap items ngoài nội dung đã duyệt — thiếu sót thì flag epic.
