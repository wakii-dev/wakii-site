# Context pack — FI-300 SF-1 Releases foundation (Tier 0)

Epic: FI-300 · Spec: docs/superpowers/specs/2026-09-04-site-downloads-mobile.md · Fork từ `story/fi300-downloads-mobile` (tạo lúc APPROVE từ head `story/fi294-site-content-depth` SAU story 2 close).

## Spec slice
SF-1 là nền chung: config flags/URLs + i18n keys FULL SET (mọi surfaces + cả 2 biến thể flag) + roadmap/docs/nav/footer copy đồng bộ + baseline Lighthouse. KHÔNG có trang mới — chỉ nền để SF-2/SF-3 dựng UI.

## Touch map
- `src/config.ts`: thêm `DOWNLOAD_URLS` (desktop), `DOWNLOADS_LIVE`, `MOBILE_STORE_URLS`, `MOBILE_LIVE` — flags default **false**, comment runbook precondition (repo public + release assets tồn tại + unsigned chạy được). KHÔNG đụng `DOC_SLUGS` (LOCKED) hay REPO_URL.
- `src/i18n/` (landing.ts hoặc file keys mới nếu tách — agent quyết theo cấu trúc có sẵn): keys FULL SET EN + VI: download page (tabs, buttons, warn G-I, follow-releases), mobile-connect (badges, QR caption, capabilities G-C, coming soon), teaser. **CẢ 2 biến thể flag cho mọi key điều kiện** (G-D draft — convergence gate duyệt sau).
- `src/data/roadmap.ts` + renderer: dòng "GitHub Releases + binaries" render **flag-aware** (G-B).
- Docs getting-started (content collection, EN+VI): note download ngắn (G-E).
- Nav + Footer: link Download (href `${prefix}/download/` — giờ dead-link tạm do route chưa có ở SF-1; route xuất hiện ở SF-2, chấp nhận trong 1 SF window — GHI vào commit message).
- Faq + ZeroSetup copy: đồng bộ theo flag ("no binaries" → điều kiện theo `DOWNLOADS_LIVE`).
- Baseline: chạy Lighthouse landing + pages hiện có, ghi số vào `docs/superpowers/notes/lighthouse-baseline-fi300.md` (commit).

## ACCEPTANCE user-visible (verifier Phase 5 kiểm từng dòng)
1. Nav + Footer có link "Download" ở CẢ 2 locale, href đúng pattern locale.
2. Roadmap: flag=false → dòng binaries nằm Next copy mới; mock flip flag=true → dòng chuyển Now/Shipped (test bằng local flip).
3. Docs getting-started (EN+VI) có note download 1-2 câu, đúng trạng thái flag hiện tại.
4. FAQ/ZeroSetup không còn claim "no binaries" tuyệt đối mà điều kiện theo flag.
5. Config có đủ 4 key mới, flags default false, runbook comment đủ 3 preconditions.
6. Keys i18n đủ cho mọi surfaces SF-2/SF-3 cần (cross-check task lists SF-2/SF-3 trong bracket) — SAU SF-1 không SF nào được thêm key vào landing.ts.
7. Baseline Lighthouse ghi file, số thực (không fabrication).
8. Build xanh cả 2 locale.

## Boundary
- KHÔNG tạo route /download (SF-2). KHÔNG đụng GetWakii.astro (SF-3). KHÔNG mobile component (SF-2).
- KHÔNG flip flag — mặc định false; flip là việc USER/manual.
- Chi tiết chưa ghi → agent tự quyết hợp lý; quyết lớn → FLAG comment lên epic FI-300.
