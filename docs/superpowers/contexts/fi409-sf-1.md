# SF-1 Context Pack — KB skeleton: structure + conventions + 10 ADR + glossary + MOC

> Epic spec: `docs/superpowers/specs/2026-09-09-knowledge-base-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi409-knowledge-base.md`.

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)

1. `docs/knowledge/` skeleton: README.md (index + conventions: biên superpowers=in-flight vs knowledge=finished · LINK-only rules · frontmatter schema authority trỏ task 9 · **worked example chain 4-link**: KB entry → research → post → issue → claim + MOC row template) · `adr/` (10 file NNNN-tên) · `glossary.md` (≥15 thuật ngữ EN/VI) · `MOC.md` (slug-level, maintenance rule vào conventions) · `lessons/index.md` (trỏ improvements-log, KHÔNG move).
2. **10 ADR VI-first MADR-ish** (naming `NNNN-<tên>.md`, status field MADR), mỗi ADR có nguồn pin:
   - Enforcing ×4: lint all-non-seed (check-blog-content.mjs:16-18) · grading marker scoped batch-3 (FI-383 D4) · license claims scoped † (claims-registry) · future-date policy (a) (audit T7)
   - Process ×4: spec-first workflow (docs/superpowers/specs/*) · matrix CHỐT CỨNG per batch (batch2-design DEC-1) · human gates (VI copy + flag flips + VERDICT APPROVED literal) · batch 2-đợt pilot-first (batch2-design D1)
   - Platform/content ×3: category enum 3 + flat listing (batch2 DEC-2/3) · learn-in-public + third-party số kèm ngày probe (claims-registry §Third-party) · hero 10 flagship PINNED (batch2-design DEC-5)
3. Frontmatter schema authority: `title/date/updated/status/visibility/confidence/sources/tags/type` — schema định nghĩa ở task 9, README chỉ link.
4. Sensitivity pass per-file (D6 — repo ĐÃ public): bảng pass/fail 100% KB files, tiêu chí: 0 evening-round competitive claim, mọi page `visibility: internal`, ADR chỉ restatement từ nguồn đã public → Linear comment.
5. Machine verify 4 cái: gray-matter parse frontmatter (throwaway) · link-check relative resolve · adr count = 10 · `git diff baseline -- ':(exclude)docs/knowledge/**'` rỗng.

## Touch map

- W: `docs/knowledge/**` (toàn bộ MỚI — additions-only)
- READ-ONLY: editorial kit (lint-pinned — KHÔNG move/sửa) · improvements-log.md (LINK-only) · research/ 99 files (SF-2 deferred move) · scripts/* · MEMORY.md ngoài repo

## ACCEPTANCE (user-visible)

- MOC từ 1 chủ đề (vd "spec-driven") truy được đầy đủ: research digest → blog post → ADOPT issue → ADR decision.
- 10 ADR đọc được VI-first, mỗi cái có nguồn pin + status MADR.
- Machine checks 4/4 pass; `git diff baseline -- ':(exclude)docs/knowledge/**'` rỗng = additions-only chứng minh.
- Sensitivity bảng 100% pass trong Linear comment.

## Boundary (KHÔNG làm)

- KHÔNG move research/ (SF-2 migration issue FI-410 riêng, sau 09-30), KHÔNG đụng editorial kit (lint-pinned), KHÔNG improvements-log (LINK-only), KHÔNG enforcement script (mức Full), KHÔNG đụng MEMORY.md ngoài repo, KHÔNG Starlight/GraphRAG.
