# SF-3 Context Pack — Series Features (8 bài)

> Đọc file này THAY VÌ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch2-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi373-blog-batch2.md`.

## Spec slice (chỉ phần SF-3 chịu trách nhiệm)

1. 8 bài product features, category `tech`, 900-1400 từ VI + EN mirror cùng commit:
   `feature-terminal-splits` (2 flagship: hero) · `feature-ssh-worktrees` · `feature-design-mode` · `feature-ai-diff-annotation` · `feature-emulator-android` · `feature-computer-use-native` (2 flagship: hero) · `feature-per-workspace-env` · `feature-notification-keyboard`
2. **ĐIỀU KIỆN TIỀN ĐẦ**: đọc nhãn verify-shipped trong claims-registry (SF-1) — mỗi bài CHỈ được claim theo nhãn: SHIPPED (trong v1.4.199) viết bình thường · MAIN-ONLY → claim "đang trên main, sắp ra release" · ROADMAP → KHÔNG viết (bỏ bài, báo coordinator).
3. Cấu trúc: feature làm gì (user-visible) → code ở đâu (sibling `/Users/hoivu/Desktop/projects/orca` READ-ONLY — trỏ file/thư mục thật) → trải nghiệm thực tế + ASCII/screenshot mô tả → giới hạn hiện tại → ≥1 link `/docs/agents-and-kit/` hoặc `/docs/faq/`.
4. pubDate theo matrix; tags từ vocab; audit T7 đỏ giữa batch EXPECTED.

## Touch map

- W: `src/content/blog/{en,vi}/feature-*.md` (16 file) · `public/blog/heroes/{feature-computer-use-native,feature-terminal-splits}.png`
- Chạy: `scripts/render-blog-heroes.mjs` (parameterized SF-1)
- READ-ONLY: `/Users/hoivu/Desktop/projects/orca/**` (sibling — code evidence), claims-registry (SF-1 frozen), `src/utils/blog.ts`

## ACCEPTANCE (user-visible)

- 8 bài features đọc được EN+VI trên listing đúng matrix date; mở từng bài thấy: feature demo được, trỏ đúng code location thật, giới hạn nói thẳng.
- Claims 100% khớp nhãn registry (bài MAIN-ONLY không nói "đã có trong release"); 2 flagship có hero; 6 không hero sạch.
- Mobile 390 no-overflow; EN↔VI switch đúng bài.

## Boundary (KHÔNG làm)

- KHÔNG đụng skills/guides/arch posts, KHÔNG sửa orca repo (sibling READ-ONLY tuyệt đối), KHÔNG sửa scripts, KHÔNG wire category-link meta (đã có từ FI-349 SF-4), KHÔNG claim vượt nhãn registry.
