# SF-6 Context Pack — Convergence — 120 slug + file ADOPT issues

> Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch3-repos-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi383-blog-batch3.md`. Template + rubric: SF-1/2 outputs.

## Spec slice (chỉ phần SF-6 chịu trách nhiệm)

1. **Strict coverage**: assert 100 file mới (50 slug × 2 locale) tồn tại trong `src/content/blog/{en,vi}/` — chống silent scope shrink (lint partial-by-design + T7 pendingRows chỉ NOTE). T7 pendingRows == 0.
2. **File TẤT CẢ ADOPT issues tập trung** lên `wakii-dev/wakii` (rubric SF-1): selection ADOPT-trước-DIRECTION theo grading distribution review (kỳ vọng ~5-10 issues); label `enhancement`; body inline evidence + retrieval dates + upstream links + link bài post (ghi rõ window 404 ngắn trước khi story merge — build-in-public chấp nhận).
3. **Probe-recheck**: chạy lại `scripts/probe-repos.sh` — repo GONE/renamed/archived giữa research và publish → draft:true hoặc bỏ (có coordinator note).
4. Full audits trên `dist/` (165→240 pages): lint all-non-seed xanh · parity --dist 120 slug · audit convergence T1-T7 (T7 pendingRows == 0) · check-jsonld (BlogPosting/Blog/Breadcrumb 120 slug) · RSS ~240 items bilingual · sitemap/hreflang full · og per-post absolute · listing flat 120×2 render EN+VI · 6 category pages audit count · related invariants 25 cũ · claims sweep third-party (license-safe scoped) · reduced-motion · browser walkthrough EN+VI mỗi nhóm repo (Rule 0 3 tầng).
5. Independent review (code-reviewer) + release readiness build smoke.

## Touch map

- W: `scripts/check-jsonld.mjs` (mở rộng assert nếu cần), fix-minor nhỏ
- Chạy: mọi scripts + `pnpm build`
- READ-ONLY: mọi content/layout/component (bug lớn thuộc SF-2..5 → escalate KHÔNG fix thay)

## ACCEPTANCE (user-visible)

- /blog/ EN+VI: 120 slug render newest-first; 6 category pages đúng count; RSS ~240 items; JSON-LD + og per-post + hreflang đầy đủ.
- ADOPT issues đã file trên wakii-dev/wakii kèm link bài post; probe-recheck không GONE mới.
- Browser walkthrough EN+VI pass (Rule 0); mọi script exit 0.

## Boundary (KHÔNG làm)

- KHÔNG viết bài mới, KHÔNG redesign, KHÔNG đụng contracts pinned (audit — drift = FAIL + escalate), KHÔNG deploy prod (owner), KHÔNG merge dest vào main (human gate), KHÔNG đụng worktrees story khác.
