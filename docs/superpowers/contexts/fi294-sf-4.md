# SF-4 Context Pack — Philosophy + landing deepening (FI-294)
> Đọc file này THAY VÌ tự tổng hợp. Epic: FI-294.
> Epic spec: docs/superpowers/specs/2026-09-04-site-content-depth.md.
> Bracket: docs/superpowers/brackets/fi294-site-content-depth.md. Tier 1 — Depends SF-1.
> Design: none — mở rộng landing theo pattern sections hiện có + sf1-direction.md
> (không cần 3-hướng designer).

## Spec slice (chỉ phần SF-4 chịu trách nhiệm)
1. **Philosophy section trên landing**: section MỚI (vị trí đề xuất: sau bento
   hoặc sau workflow — quyết định khi implement theo flow đọc, ghi notes) gồm 8
   pillar cards rút gọn (tên + 1 câu — copy+keys đã có từ SF-1 trong landing.ts)
   + CTA/link "đọc đầy đủ" → heading anchor của philosophy trong
   `src/content/docs/{en,vi}/story-workflow.md`.
2. **Pillar heading anchor**: đảm bảo heading "Philosophy" trong story-workflow.md
   (EN+VI) có id anchor để link sâu (nếu chưa có → thêm id vào heading; KHÔNG
   link theo số dòng). Lưu ý VI fallback-by-construction: nếu VI doc thiếu thì
   anchor vẫn hoạt động trên bản EN render.
3. **Workflow deepen**: enrich component workflow (tách từ section `#workflow`
   hiện có — stage strip idea→impact→plan→parallel SFs→verify gates→1 PR) để
   giải thích CÁCH LÀM VIỆC: 9 agents (bảng trong agents-and-kit.md là nguồn),
   gates B0-B5 kiểm gì, con người duyệt cái gì (humans-own-irreversibles). Copy
   keys pre-add từ SF-1; chỗ này wiring markup trong component mình sở hữu.
4. Entrance animations: TIÊU THỤ motion util SF-1 nguyên trạng.
5. Responsive pass cho 2 section (8 cards × mobile, workflow strip).

## Touch map (SF-4 sở hữu)
- `src/components/landing/philosophy.astro` (MỚI — SF-4 sở hữu)
- `src/components/landing/workflow.astro` (componentized bởi SF-1 — SF-4 sở hữu
  editing, chạy SAU khi SF-1 merged)
- `src/i18n/landing.ts` — KHÔNG sửa (keys pre-add từ SF-1; thiếu key = flag epic)
- `src/content/docs/en/story-workflow.md`, `vi/story-workflow.md` — CHỈ thêm id
  anchor vào heading (không sửa nội dung khác)
- READ-ONLY: motion util, data modules, mockups kit, các landing components khác
  (hero/bento/get-wakii của SF-3)

Design rationale (Design: none — GHI VÀO NOTES khi implement): pillar cards
reuse established bento card language của sf1-direction.md (grid card cùng hệ
tokens) — không cần designer 3-hướng; nếu thấy cần break visual language mới
flag epic.

## ACCEPTANCE (user-visible)
1. Landing (EN + VI) có philosophy section: 8 trụ cột đọc được, click "đọc đầy
   đủ" nhảy đúng vị trí triết lý trong docs story-workflow (không 404, đúng
   locale).
2. #workflow giải thích được: ai làm gì (9 agents), gates kiểm gì, con người
   duyệt gì — đọc xong hiểu cách team làm việc không cần mở docs.
3. Cards + strip reveal mượt qua util; reduced-motion tắt sạch.
4. Mobile: 8 pillar cards + workflow strip xếp gọn không vỡ layout.

## Boundary (KHÔNG làm)
- KHÔNG sao chép full 8-pillar copy từ docs vào landing (DRY — landing rút gọn).
- KHÔNG đụng skills catalog (SF-2), get-wakii/roadmap (SF-3).
- KHÔNG sửa nội dung docs ngoài id anchor.
- KHÔNG thêm motion primitive mới (SF-5).
- KHÔNG đổi thứ tự/số lượng section hiện có ngoài thêm philosophy section.
