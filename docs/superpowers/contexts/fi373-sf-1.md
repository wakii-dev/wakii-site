# SF-1 Context Pack — Editorial infra: scope manifest + claims refresh

> Đọc file này THAY VÌ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch2-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi373-blog-batch2.md`.

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)

1. `scripts/check-blog-content.mjs` refactor: bỏ `NEW_SLUGS` hard-code → đọc **manifest all-non-seed** (mọi slug trong `src/content/blog/{en,vi}/` KHÔNG thuộc 5 seed FI-341: review-ai-agents-from-your-phone / story-workflow-idea-to-release / decision-gates-safe-ai-agents / forking-an-ide-keeping-current-with-upstream / building-wakii-in-the-open-log-1). Exit: bài giả lập sai band thuộc manifest BỊ CHẶN; file lạ ngoài manifest BỊ BÁO.
2. `scripts/audit-blog-convergence.mjs` refactor **6 điểm hard-code**: T5 `expectedLive` (derived, trừ draft×2) · T7 `cards.length` (live-derived) · T7 `rows.size` (→ 64 non-seed) · `SNAPSHOT` object (skills **20/13** ground truth `src/data/skills.ts` + releases v1.4.198/199) · T3 `FAMILIES` (+ family batch-2: skill-*, feature-*, guide-*, arch-*, oss-*) · T6 `SAMPLES` (sample theo facet batch-2 hoặc full-sweep 64×2).
3. **Future-date policy (a)**: T7 line ~709 `> TODAY` → future-date chỉ FAIL khi pubDate ∉ matrix (matrix = nguồn chân lý, đọc từ 2 bảng topic-matrix.md + matrix batch-2 theo regex `| N | slug | cat | pubDate |`).
4. Claims-registry + evidence-pack §Numbers refresh **CÙNG commit, cùng ngày chụp** (audit T3 trỏ cả hai file): skills 20/13, releases, thêm nhãn verify-shipped.
5. **Verify-shipped 8 features** (đối chiếu `/Users/hoivu/Desktop/projects/orca` READ-ONLY + release v1.4.199): terminal-splits / ssh-worktrees / design-mode / ai-diff-annotation / emulator-android / computer-use-native / per-workspace-env / notification-keyboard → nhãn SHIPPED / MAIN-ONLY / ROADMAP kèm bằng chứng (commit/release ref). Artifact drift-note bắt buộc cho probe skills-drift (21/14 → 20/13 — finding + file:line).
6. Hero pipeline parameterize: `render-blog-heroes.mjs` derive slug từ frontmatter scan (`heroImage` frontmatter → `public/blog/heroes/<slug>.png`) thay vì hard-code `HERO_SLUGS`; demo fixture: post tạm → render 1 PNG 1200×630 → assert → revert. KHÔNG render batch-2 (posts chưa tồn tại).
7. Matrix 44 slug **table-format** commit (regex parse được: `| N | slug | cat | pubDate |`) — pubDate 09-09→09-30, 2/ngày.
8. Style guide tags vocab mở rộng (tags mới cho skills/features/arch facet) + runbook update.

## Touch map

- W: `scripts/check-blog-content.mjs` · `scripts/audit-blog-convergence.mjs` · `scripts/render-blog-heroes.mjs` · `docs/superpowers/editorial/2026-blog-longform/{claims-registry,evidence-pack,style-guide,runbook}.md` · matrix batch-2 file mới (cùng thư mục editorial) · `public/blog/heroes/` (fixture only, revert)
- READ-ONLY: `src/content.config.ts` (enum 3 không đổi) · `src/pages/*` (SF-3+ sở hữu pagination? KHÔNG — DEC-3 flat) · `src/data/skills.ts` (ground truth, không sửa)
- Sau SF-1: editorial docs FROZEN — thêm mục qua coordinator

## ACCEPTANCE (user-visible)

- Viết thử 1 bài batch-2 giả lập sai word-band → `pnpm build` CHẶN với lỗi lint rõ ràng (không skip im lặng).
- File .md lạ trong tree không thuộc manifest → lint/audit BÁO danh sách.
- `node scripts/audit-blog-convergence.mjs` — future-date ∈ matrix PASS; mọi con số SNAPSHOT khớp skills.ts 20/13 + releases, kèm ngày chụp.
- 8 nhãn verify-shipped có bằng chứng trong claims-registry; drift-note skills có file:line.
- Fixture hero render ra PNG 1200×630 rồi reverted sạch.

## Boundary (KHÔNG làm)

- KHÔNG viết bài nội dung (SF-2..5), KHÔNG render hero batch-2, KHÔNG đụng category routes/listing (DEC-3 flat), KHÔNG đổi enum/contracts/RSS, KHÔNG astro:assets.
