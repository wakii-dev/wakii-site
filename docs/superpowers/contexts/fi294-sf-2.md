# SF-2 Context Pack — Skills catalog page (FI-294)
> Đọc file này THAY VÌ tự tổng hợp. Epic: FI-294.
> Epic spec: docs/superpowers/specs/2026-09-04-site-content-depth.md.
> Bracket: docs/superpowers/brackets/fi294-site-content-depth.md. Tier 1 — Depends SF-1.
> **Design-first**: bracket ghi `Design: mock-prototype` — designer phase (3 hướng
> HTML → user pick → hand-off docs/superpowers/designs/<slug>-direction.md) CHẠY
> TRƯỚC dev. Visual authority: sf1-direction.md (tokens mint #45E0A8, near-black
> #0A0E0D, JetBrains Mono + Inter) — aihero.dev chỉ là CONTENT pattern.

## Spec slice (chỉ phần SF-2 chịu trách nhiệm)
1. **PRE-CONDITION: PM đã confirm triage list của SF-1** (không confirm = KHÔNG
   start — escalate coordinator). Route `/skills` (EN) + `/vi/skills` (VI) —
   plain Astro pages, KHÔNG qua docs collection (DOC_SLUGS không đụng). Render
   từ data module SF-1 (`src/data/skills.ts` hoặc `src/i18n/skills.ts` — đọc
   code SF-1 merged), LỌC THEO TRIAGED SUBSET (không render skill đã loại).
2. Layout kiểu aihero /skills: header + install strip + skills NHÓM THEO CATEGORY
   (taxonomy từ notes SF-1), mỗi skill = card mở rộng: command (mono), name,
   desc (1 câu), how-it-works (2-3 câu). VI song song EN.
3. Install strip: lệnh build-from-source + link `/docs/getting-started/` (KHÔNG
   link #get-wakii — section đó của SF-3, chưa tồn tại lúc SF-2 demo).
4. Entrance animations: TIÊU THỤ motion util SF-1 nguyên trạng (data-attrs) —
   KHÔNG viết primitive mới. Reduced-motion tự sạch nhờ util.
5. SEO/hreflang cho route mới: verify `Base.astro` canonical/hreflang math hoạt
   động đúng cho non-/docs top-level route; nếu cần điều chỉnh, sửa Base một cách
   an toàn cho TẤT CẢ pages (regression check 13 pages cũ).
6. Cross-link: `agents-and-kit.md` (EN+VI) thêm link sang /skills.

## Touch map (SF-2 sở hữu)
- `src/pages/skills.astro` (MỚI), `src/pages/vi/skills.astro` (MỚI)
- Design hand-off: `docs/superpowers/designs/sf2-skills-direction.md` (MỚI, từ
  designer phase)
- `src/layouts/Base.astro` — CHỈ nếu hreflang cần fix cho route shape mới
- `src/content/docs/en/agents-and-kit.md` + `vi/` — thêm 1 link
- READ-ONLY: `src/data/skills.ts` (SF-1 sở hữu — bug thì flag, không sửa lặng lẽ),
  `src/components/motion.ts` (tiêu thụ), mockups kit, DOC_SLUGS

## ACCEPTANCE (user-visible)
1. Vào /skills (và /vi/skills) từ nav: thấy skills đã triage nhóm theo category,
   mỗi skill đọc được mô tả + cách làm việc bằng ngôn ngữ đó.
2. Install strip hiện lệnh build-from-source, click dẫn tới getting-started.
3. Scroll trang: cards reveal mượt; bật prefers-reduced-motion → tắt sạch.
4. Lang switcher EN↔VI trên /skills hoạt động đúng (URL map 2 chiều).
5. View-source: canonical + hreflang en/vi đúng cho /skills và /vi/skills.

## Boundary (KHÔNG làm)
- KHÔNG build per-skill detail pages (/skills/<slug>) — non-goal đã chốt.
- KHÔNG sửa data module nội dung (SF-1 sở hữu) — thiếu sót flag lên epic.
- KHÔNG đụng landing sections, quickstart/get-wakii (SF-3), roadmap (SF-3).
- KHÔNG thêm motion primitive mới / motion cho pages khác (SF-5).
- KHÔNG đổi docs collection schema hay DOC_SLUGS.
