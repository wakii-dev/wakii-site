# Context pack — FI-300 SF-2 Download page + mobile connect component (Tier 1)

Epic: FI-300 · Spec: docs/superpowers/specs/2026-09-04-site-downloads-mobile.md · Fork từ `story/fi300-downloads-mobile`. Depends: SF-1 (flags/keys đã có — KHÔNG thêm key vào landing.ts).

## Spec slice
Trang `/download` EN + `/vi/download` + component mobile-connect (dùng chung bởi SF-3 teaser). Design phase mock-prototype CHẠY PRE-LAUNCH — hand-off `docs/superpowers/designs/sf-downloads-direction.md` là source of truth visual; dev tasks bên dưới dev-only, implement 1:1 theo hand-off.

## Touch map
- `src/pages/download.astro` (EN, không prefix) + `src/pages/vi/download.astro` (VI) — theo đúng pattern pages có sẵn.
- `src/components/download/MobileConnect.astro` (tên agent quyết) — component prop-driven, nhận locale + flags qua config, KHÔNG hardcode copy.
- Platform tabs: manual-first (tab là links/checkbox-hack, KHÔNG JS-only), no-JS fallback người dùng không JS vẫn thấy nút cho platform của mình (hoặc cả 2).
- Download buttons: đọc `DOWNLOAD_URLS` + `DOWNLOADS_LIVE`; flag=false → KHÔNG render button, render build-from-source prominent + link Follow releases (GitHub Releases page). Flag=true → buttons + warn copy G-I (unsigned: macOS Privacy & Security / Windows SmartScreen).
- MobileConnect block: badges iOS/Android; `MOBILE_LIVE=false` → coming soon + follow updates, KHÔNG QR, KHÔNG store badge giả; `MOBILE_LIVE=true` → store links + QR (G-QR: encode store URL mặc định; /app CHỈ nếu trang tồn tại — hiện out-of-scope). Capabilities copy theo G-C (zero-default nếu user không duyệt gì).
- SEO: title/desc/canonical/hreflang ×2 route (pattern như docs pages).
- Entrance animations: qua motion util có sẵn (`src/components/motion.ts`, data-reveal + initMotion) — KHÔNG viết animation mới.
- Responsive pass cả 2 route (iframe probe w=375/390 thay headless screenshot — gotcha clamp).

## ACCEPTANCE user-visible (verifier Phase 5 kiểm từng dòng)
1. `/download` + `/vi/download` render đúng, hreflang/canonical đối chiếu nhau.
2. Flag=false (hiện tại): trang hiển thị build-from-source + follow releases, KHÔNG có nút download chết, KHÔNG QR, KHÔNG store link.
3. Mock flip `DOWNLOADS_LIVE=true`: nút .dmg/.exe render theo đúng `DOWNLOAD_URLS`, warn copy G-I hiện (EN+VI đúng locale).
4. Mock flip `MOBILE_LIVE=true`: badges → store links, QR render, caption đúng mức claim G-QR.
5. No-JS: tắt JS vẫn thấy đầy đủ content + nút (tabs fallback).
6. Reduced-motion: animations tôn trọng prefers-reduced-motion (qua util có sẵn).
7. Component MobileConnect import được độc lập (SF-3 sẽ reuse — prop interface rõ).
8. Build xanh + Lighthouse /download ≥95 (absolute — trang mới).

## Boundary
- KHÔNG sửa GetWakii/Landing (SF-3). KHÔNG thêm i18n keys (SF-1 sở hữu — thiếu key → FLAG epic). KHÔNG đặt QR encode pairing endpoint. KHÔNG flip flags thật.
- Chi tiết chưa ghi → theo hand-off design + tự quyết hợp lý; quyết lớn → FLAG epic FI-300.
