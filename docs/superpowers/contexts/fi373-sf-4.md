# SF-4 Context Pack — Series Guides (10 bài)

> Đọc file này THAY VÌ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch2-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi373-blog-batch2.md`.

## Spec slice (chỉ phần SF-4 chịu trách nhiệm)

1. 10 bài guides, category `tutorial`, 900-1400 từ VI + EN mirror cùng commit (fenced code không tính vào band — **kế hoạch độ dài ngay từ đầu**, đừng đệm chữ):
   `guide-install-update` · `guide-first-story-end-to-end` (flagship: hero) · `guide-mobile-pairing` · `guide-ssh-remote` · `guide-custom-skill-101` · `guide-cloud-relay` · `guide-worktree-workflow` · `guide-linear-github-wiring` · `guide-multi-session-ports` · `guide-troubleshooting`
2. Cấu trúc: mục tiêu đầu bài → prerequisites → các bước có lệnh thật → kết quả mong đợi → troubleshooting ngắn → ≥1 link `/docs/getting-started/` hoặc `/docs/faq/`.
3. **Exit criterion guides** (khác content SF khác): mỗi guide — commands cross-check vs docs/CLI hiện hành + ≥1 lệnh dry-run thật kèm transcript; **walkthrough máy sạch = user-manual gate sau merge** (không verify autonomous được — không hứa "đã test máy sạch").
4. pubDate theo matrix; audit T7 đỏ giữa batch EXPECTED.

## Touch map

- W: `src/content/blog/{en,vi}/guide-*.md` (20 file) · `public/blog/heroes/guide-first-story-end-to-end.png`
- Chạy: `scripts/render-blog-heroes.mjs`, `scripts/check-blog-content.mjs`
- READ-ONLY: docs pages (`src/content/docs/*` — cross-check commands), claims-registry, `src/utils/blog.ts`

## ACCEPTANCE (user-visible)

- 10 guides đọc được EN+VI, mỗi bài đi được các bước không thiếu khoảng trống; lệnh trong bài khớp CLI/docs hiện hành (cross-check pass).
- Flagship guide có hero; còn lại sạch fallback; mobile 390 no-overflow; EN↔VI đúng bài.
- Transcript dry-run tồn tại trong plan/PR cho từng guide.

## Boundary (KHÔNG làm)

- KHÔNG đụng skills/features/arch posts, KHÔNG sửa docs pages (`src/content/docs/*` — chỉ cross-check), KHÔNG sửa scripts, KHÔNG claim "đã kiểm trên máy sạch" (đó là user-manual gate).
