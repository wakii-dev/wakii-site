# SF-5 Context Pack — Animation pass + convergence QA (FI-294)
> Đọc file này THAY VÌ tự tổng hợp. Epic: FI-294.
> Epic spec: docs/superpowers/specs/2026-09-04-site-content-depth.md.
> Bracket: docs/superpowers/brackets/fi294-site-content-depth.md.
> Tier 2 — Depends SF-2, SF-3, SF-4 (tier-gate: CHỈ test thứ các tier trước cung
> cấp; SF này là convergence của story).

## Spec slice (chỉ phần SF-5 chịu trách nhiệm)
1. **Copy review gate EN + VI — TRƯỚC TẤT CẢ**: toàn bộ copy mới của story
   (skills data ×2, roadmap ×2, philosophy/workflow ×2, get-wakii ×2) đưa user
   review qua epic comment/artifact (VI là trọng tâm — khối lượng dịch; EN
   review cùng lượt); sửa theo feedback trước khi làm motion pass.
2. **Baseline**: đã đo ở SF-1 (đọc notes SF-1) — acceptance là perf ≥ baseline
   ĐÓ, không đo lại ở SF-5 (tránh hấp thụ regression SF-2/3/4).
3. **Landing motion deep pass**: scroll-triggered reveals per-section, stagger
   upgrade cho sections mới (philosophy 8 cards, get-wakii, workflow deepen) —
   MỞ RỘNG motion util (SF duy nhất được phép), vẫn transform/opacity only.
4. **Docs light reveals**: reveal tinh tế cho docs pages (prose sections fade-in
   nhẹ, DocsLayout) — design-system first cho docs, giữ đọc là chính.
5. **Reduced-motion audit toàn site**: mọi animation mới (SF-2/3/4 + của SF-5)
   bị kill-switch `global.css:65` tắt sạch — verify bằng emulate reduced motion.
6. **Perf audit**: Lighthouse ≥ baseline trên / và /vi/; mọi anim mới chỉ
   transform/opacity (grep verify không có layout-thrash property animate).
7. **Link integrity full site**: mọi internal link (kể cả cross-links mới: nav
   Skills/Roadmap, agents-and-kit → /skills, philosophy → anchor) resolve đúng
   cả 2 locales; không link số dòng.
8. **Locale e2e trang mới**: /skills ↔ /vi/skills, /roadmap ↔ /vi/roadmap lang
   switcher 2 chiều; VI fallback không 404.
9. **Release readiness**: build xanh, deploy preview (nếu hook Vercel), smoke
   toàn pages.

## Touch map (SF-5 sở hữu)
- `src/components/motion.ts` — MỞ RỘNG (duy nhất SF này được)
- `src/components/landing/*.astro` — motion attributes per-section (không đổi
  copy; Landing.astro shell chỉ read)
- `src/layouts/DocsLayout.astro` + docs pages — light reveal wiring
- `src/styles/global.css` / `tokens.css` — CHỈ nếu cần motion tokens (giữ
  kill-switch nguyên tắc hoạt động)
- READ-ONLY: mọi nội dung copy (SF-2/3/4 sở hữu — lỗi copy thì report, không
  sửa lặng lẽ khi sau VI gate), data modules

## ACCEPTANCE (user-visible)
1. Copy VI mới đã qua mắt user — user confirm trong epic (gate binary).
2. Bật reduced-motion → toàn site đứng im hoàn toàn (không còn animation nào).
3. Tắt reduced-motion → landing mượt hơn rõ rệt (sections reveal theo scroll,
   stagger đẹp), docs có reveal nhẹ không phá việc đọc.
4. Lighthouse / và /vi/ ≥ baseline đầu SF-5.
5. Click qua mọi link mới cả 2 locales — không chết, không 404, lang switch
   chuẩn.
6. `npm run build` xanh + preview deploy smoke pass.

## Boundary (KHÔNG làm)
- KHÔNG đổi nội dung/copy (trừ sửa theo VI review feedback).
- KHÔNG thêm pages/routes mới.
- KHÔNG sửa DOC_SLUGS/REPO_URL/config contracts.
- KHÔNG đụng mockup kit markup (chỉ data-attrs nếu cần).
- Tier-gate: KHÔNG tự bổ trợ thiếu sót của tier trước — bug SF-2/3/4 report về
  epic để SF đó xử lý (rollback/worker pattern), trừ fix cosmetic 1 dòng.
