# SF-4 Context Pack — Multi-agent + inference remainder (14 bài)

> Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch3-repos-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi383-blog-batch3.md`. Template: ĐÃ CHỐT ở SF-2 — implement theo, không tự chế.

## Spec slice (chỉ phần SF-4 chịu trách nhiệm)

1. 14 bài theo template, category `tech`, 900-1400 từ VI + EN mirror cùng commit, pubDate theo matrix:
   - Multi-agent ×9: `-langchain-ai-langchain` (145k★) · `-foundationagents-metagpt` (70k★) · `-microsoft-autogen` (60k★, CC-BY-4.0 — quote được với attribution) · `-crewaiinc-crewai` (58k★) · `-hkuds-nanobot` (47k★) · `-openai-openai-agents-python` (29k★) · `-qwenlm-qwen-agent` (17k★) · `-pydantic-pydantic-ai` (19k★) · `-camel-ai-camel` (17k★) · `-hkuds-deepcode` (16k★)
   - Inference ×3: `-ggml-org-llama-cpp` (127k★) · `-vllm-project-vllm` (91k★) · `-exo-explore-exo` (47k★)
   - Terminal ×2: `-junegunn-fzf` (flagship: hero) · `-ast-grep-ast-grep` (15k★)
2. Mỗi bài: research (gh api kèm retrieval date + README + skim code) → section `## Wakii học được gì` (grading) → ADOPT issue draft per-repo (KHÔNG file) → ≥1 docs link.
3. Hero: ollama + llama.cpp wire heroImage 2-locale + render (flagship của SF này; fzf thuộc SF-5).
4. Consistency pass: tone/structure khớp pilot, cross-links ∈ matrix 64, lint green (T7 đỏ giữa batch EXPECTED).

## Touch map

- W: `src/content/blog/{en,vi}/deep-dive-<14 repo>.md` (28 file) · `public/blog/heroes/{ollama,llama-cpp}.png`
- Chạy: `scripts/render-blog-heroes.mjs` (parameterized), `scripts/check-blog-content.mjs`
- READ-ONLY: scripts, editorial docs (frozen), template pilot

## ACCEPTANCE (user-visible)

- 14 bài đọc được EN+VI đúng template; grading section rõ ràng có dẫn chứng code/repo thật.
- 2 hero render đẹp 1200×630; mobile 390 no-overflow; lint green partial.

## Boundary (KHÔNG làm)

- KHÔNG file issues lên GitHub (SF-6), KHÔNG đụng SF-3/5 posts, KHÔNG sửa scripts/editorial docs (frozen), KHÔNG đổi matrix.
