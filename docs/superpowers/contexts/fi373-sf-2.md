# SF-2 Context Pack — Series Skills (13 bài)

> Đọc file này THAY VÌ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch2-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi373-blog-batch2.md`.

## Spec slice (chỉ phần SF-2 chịu trách nhiệm)

1. 13 bài deep-dive, ĐÚNG THỨ TỰ category, mỗi bài 900-1400 từ VI + EN mirror **cùng commit** (band thuật toán D1, hard-fail >1470 — fenced code không tính):
   - Workflow ×4: `skill-brainstorm` · `skill-writing-plans-linear` · `skill-story-workflow` · `skill-orca-superpowers-workflow`
   - Design ×6: `skill-frontend-design` · `skill-gpt-taste` · `skill-design-taste-frontend` · `skill-image-to-code` · `skill-mock-prototype` · `skill-web-design-guidelines`
   - Reference ×3: `skill-figma-orientation` · `skill-graph-engineering` · `skill-prompt-master`
2. Cấu trúc mỗi bài: command + nó làm gì → **cách làm việc nội bộ (đọc source skill thật tại kit làm evidence — KHÔNG paraphrase docs)** → khi nào dùng/không dùng → ví dụ thật từ stories đã chạy (FI-339/349/359) → ≥1 link `/docs/<slug>/` đúng locale.
3. **Hero: chỉ 5 flagship** — brainstorm, story-workflow, orca-superpowers-workflow, frontend-design, mock-prototype: frontmatter `heroImage: /blog/heroes/<slug>.png` ở CẢ EN+VI (parity presence+value) + chạy `node scripts/render-blog-heroes.mjs` (đã parameterize từ SF-1) → 8 bài còn lại KHÔNG hero (fallback og-default).
4. Category: `tech` cho cả 13; tags từ vocab style-guide; `draft: false`; pubDate theo matrix (09-09→09-30, 2/ngày, thứ tự giữ trong matrix).
5. Audit T7 ĐỎ giữa batch là EXPECTED (matrix slugs chưa đủ file) — chỉ cần lint green + cross-links ∈ matrix 64.

## Touch map

- W: `src/content/blog/en/skill-*.md` + `src/content/blog/vi/skill-*.md` (26 file) · `public/blog/heroes/<5 slug>.png`
- Chạy (không sửa): `scripts/render-blog-heroes.mjs` (SF-1 parameterized), `scripts/check-blog-content.mjs`
- READ-ONLY: `src/utils/blog.ts` · `src/i18n/categories.ts` · editorial docs (frozen sau SF-1) · source skills thật (kit — evidence)

## ACCEPTANCE (user-visible)

- /blog/ EN+VI: 13 bài skills hiện đúng thứ tự matrix, badge tech, reading-time có mặt.
- Mở 1 bài skill: thấy command, cách làm việc nội bộ (không phải paraphrase docs), ví dụ story thật, link docs đúng locale; 5 flagship có hero ảnh 1200×630 không vỡ layout; 8 bài không hero vẫn sạch.
- EN↔VI switch giữ đúng bài; mobile 390 no-overflow.

## Boundary (KHÔNG làm)

- KHÔNG đụng features/guides/arch posts (SF-3/4/5), KHÔNG sửa scripts (SF-1 frozen), KHÔNG wire category-link meta (SF-4 FI-349 đã làm — đã có trên dest), KHÔNG đổi taxonomy/enum, KHÔNG self-approve (code-reviewer độc lập bắt buộc).
