# SF-6 Context Pack — Convergence QA — 69 slug release-ready

> Đọc file này THAY VÌ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch2-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi373-blog-batch2.md`.

## Spec slice (chỉ phần SF-6 chịu trách nhiệm)

1. **Full-scale audits** (mọi script exit 0 trên `dist/` sau `pnpm build`):
   - `scripts/check-blog-content.mjs` (lint all-non-seed) — band 44 bài mới đạt, claims không vượt snapshot, docs-link ≥1/bài
   - `scripts/audit-blog-convergence.mjs` — T7 future-date ∈ matrix PASS, rows.size = 64, SNAPSHOT khớp, FAMILIES đầy đủ
   - `scripts/check-blog-slug-parity.mjs --dist` (post-build mode) — parity 69 slug EN↔VI + route tồn tại
   - `scripts/check-jsonld.mjs` — BlogPosting 138 post pages + Blog listing + BreadcrumbList 6 category, URLs absolute
   - `scripts/check-blog-utils.mjs` — readingtime/related assertions (đã mở rộng pool 69)
2. **Audits bổ sung (tự viết script nhỏ hoặc grep dist)**: RSS ~138 `<item>` bilingual guid absolute + no `<language>` · sitemap chứa mọi route (69×2 posts + 6 taxonomy + listing×2) · hreflang cluster đủ en/vi/x-default không trỏ 404 · og per-post absolute (hero/og-default theo frontmatter) · 6 category pages render đúng count theo frontmatter tổng hợp (EN+VI) · listing flat 69×2 render + JSON-LD Blog.
3. **Related-posts invariants trên 25 bài cũ** (KHÔNG assert output-freeze — output SẼ đổi khi 44 bài mới vào pool, đúng kỳ vọng): ≥1 related không rỗng, mọi related link resolve, không draft leak, cùng locale.
4. **Browser walkthrough EN+VI theo facet** (Rule 0 3 tầng): /blog/ → category tech → bài skills → bài features → prev/next → related → lang-switch → về; listing/category/detail screenshots; reduced-motion check tại viewer; mobile 390 no-overflow.
5. **Independent review**: dispatch code-reviewer trên diff toàn story + 1 verifierPASS/PARTIAL/FAIL trên ACCEPTANCE này.
6. **Release readiness**: `astro build` xanh + mọi script trên exit 0 + tổng kết announce-ready (LCP deployed = post-merge manual step — KHÔNG criterion ở đây, local preview chỉ ±8-17 noise).

## Touch map

- W: `scripts/check-jsonld.mjs` (nếu cần mở rộng assert), audit scripts nhỏ (fix-minor nếu audit bắt lỗi nhỏ thuộc SF-2..5 → lớn thì escalate SF đó)
- Chạy (đọc): toàn bộ scripts + `pnpm build`
- READ-ONLY: mọi content/layout/component (bug lớn thuộc SF-2..5 → escalate, KHÔNG fix thay)

## ACCEPTANCE (user-visible)

- /blog/ EN+VI: đủ 25+44=69 slug render, newest-first (bài future-dated 09-30 đầu — đúng policy), badge/date/reading-time đầy đủ.
- 6 taxonomy pages: count khớp frontmatter tổng hợp, EN+VI mirror.
- 1 post bất kỳ mỗi facet: docs-link hoạt động, related đúng cụm, TOC, hero (flagship) hoặc sạch (fallback), JSON-LD + og đúng.
- RSS 138-ish items bilingual; sitemap đầy đủ; mọi audit script exit 0; walkthrough EN+VI screenshots kèm verdict.

## Boundary (KHÔNG làm)

- KHÔNG viết bài mới, KHÔNG redesign thêm, KHÔNG đụng contracts pinned (chỉ audit — drift = FAIL + escalate rollback-fixer), KHÔNG deploy prod (owner), KHÔNG merge dest vào main (human gate), KHÔNG đụng FI-359 worktrees (story song song nếu còn).
