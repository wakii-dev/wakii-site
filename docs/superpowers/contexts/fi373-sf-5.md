# SF-5 Context Pack — Series Architecture + OSS + Logs (13 bài)

> Đọc file này THAY VÌ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch2-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi373-blog-batch2.md`.

## Spec slice (chỉ phần SF-5 chịu trách nhiệm)

1. 11 bài arch/OSS, category `tech`, 900-1400 từ VI + EN mirror cùng commit:
   `arch-electron-process-model` (flagship: hero) · `arch-relay-cloud` · `arch-native-computer-use` · `arch-site-bento-tokens` · `arch-site-motion-i18n` · `arch-auto-update-feed` · `oss-why-fork-mit` · `oss-upstream-sync` · `oss-brand-monogram` · `oss-release-roundup-14x` · `arch-ci-gates`
   + 2 build-logs category `build-log`: `building-wakii-in-the-open-log-3` · `building-wakii-in-the-open-log-4`
2. Evidence nguồn (sibling `/Users/hoivu/Desktop/projects/orca` + repo site — READ-ONLY): electron `src/{main,preload,renderer,relay,shared,types}` + `electron.vite.config.ts`; cloud `cloud/{apps,infra,packages}` + relay-fence-broker/relay-ops; native `native/computer-use-{macos,windows,linux}` + keyboard-layout/notification-status; site `src/styles/tokens.css`, motion, i18n, `src/pages/robots.txt.ts`, GitHub Actions; brand `public/wakii-icon.svg` + icon pipeline; releases v1.4.198/199 (`gh release view`).
3. **Build-logs evidence-floor**: chỉ cite arc/SF ĐÃ hoàn tất (FI-349 STORY-COMPLETE + PR #2; FI-359 STORY-COMPLETE + PR #3 — đều đã merge) + số từ snapshot SF-1 kèm ngày. Không đủ evidence → `draft:true` (tự loại khỏi listing/RSS/related). KHÔNG cite SF batch-2 đang chạy dở.
4. Mỗi bài: kiến trúc/decisions thật (trỏ file thật), trade-offs, diagram ASCII nếu giúp; ≥1 link `/docs/` phù hợp (agents-and-kit/faq thường khớp).
5. pubDate theo matrix (log-3/log-4 cuối lịch 09-29/09-30); audit T7 đỏ giữa batch EXPECTED.

## Touch map

- W: `src/content/blog/{en,vi}/{arch-*,oss-*}.md` (22 file) + `{en,vi}/building-wakii-in-the-open-log-{3,4}.md` (4 file) · `public/blog/heroes/arch-electron-process-model.png`
- Chạy: `scripts/render-blog-heroes.mjs`, `scripts/check-blog-content.mjs`
- READ-ONLY: `/Users/hoivu/Desktop/projects/orca/**`, `src/styles/*`, `.github/workflows/*`, claims-registry (frozen)

## ACCEPTANCE (user-visible)

- 13 bài đọc được EN+VI; bài arch có diagram/sơ đồ ASCII + trỏ đúng file thật trong repo (người đọc tìm được code theo đường dẫn trong bài).
- Build-logs kể chuyện có số liệu đúng snapshot (ngày chụp ghi trong bài), không bịa transcript.
- Flagship arch có hero; còn lại sạch; mobile 390 no-overflow; EN↔VI đúng bài.

## Boundary (KHÔNG làm)

- KHÔNG đụng skills/features/guides posts, KHÔNG sửa orca repo, KHÔNG sửa scripts/site code (chỉ viết content), KHÔNG cite story/sf đang chạy dở, KHÔNG dùng số stale không kèm ngày.
