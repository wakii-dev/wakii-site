# SF-3 Context Pack — Harness + MCP remainder (10 bài)

> Epic spec: `docs/superpowers/specs/2026-09-08-blog-batch3-repos-design.md` (rev 3). Bracket: `docs/superpowers/brackets/fi383-blog-batch3.md`. Template: ĐÃ CHỐT ở SF-2 (`docs/superpowers/editorial/research/digests-batch3/` + style-guide template section) — implement theo, không tự chế.

## Spec slice (chỉ phần SF-3 chịu trách nhiệm)

1. 10 bài theo template, category `tech`, 900-1400 từ VI + EN mirror cùng commit, pubDate theo matrix:
   - Harness remainder ×5: `deep-dive-openhands-openhands` (86k★) · `-aaif-goose-goose` (54k★) · `-aider-ai-aider` (48k★) · `-charmbracelet-crush` (27k★) · `-xai-org-grok-build` (26k★)
   - MCP remainder ×5: `-modelcontextprotocol-registry` (7k★†) · `-microsoft-mcp-for-beginners` (17k★) · `-hangwin-mcp-chrome` (12k★) · `-deusdata-codebase-memory-mcp` (42k★) · `-headroomlabs-ai-headroom` (70k★)
2. Mỗi bài: research (gh api: stars/releases/license/pushed kèm retrieval date + README + skim code) → VI+EN → section `## Wakii học được gì` (grading ADOPT/DIRECTION/WATCH/N/A so surface thật) → ADOPT issue draft per-repo (KHÔNG file) → ≥1 docs link.
3. License † (registry): "công khai trên GitHub", KHÔNG "open-source".
4. Không flagship hero — fallback og-default.
5. Consistency pass: tone/structure khớp 12 bài pilot, cross-links ∈ matrix 64, lint green (partial-tolerant — T7 đỏ giữa batch EXPECTED).

## Touch map

- W: `src/content/blog/{en,vi}/deep-dive-<10 repo>.md` (20 file) · `docs/superpowers/editorial/research/digests-batch3/adopt-draft-*.md` (10)
- READ-ONLY: scripts (frozen), editorial docs (frozen), template pilot (SF-2 output)

## ACCEPTANCE (user-visible)

- 10 bài đọc được EN+VI đúng template pilot (section grading rõ, evidence trích dẫn có nguồn + ngày).
- ADOPT drafts đủ 10, body theo rubric SF-1.
- Lint green; cross-links ∈ matrix; mobile 390 no-overflow.

## Boundary (KHÔNG làm)

- KHÔNG file issues lên GitHub (SF-6), KHÔNG đụng skills/features/guides posts (stories khác), KHÔNG sửa scripts/editorial docs (frozen), KHÔNG đổi matrix.
